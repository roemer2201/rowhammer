.claude/
# Debian build artifacts (dpkg-buildpackage / debhelper litter and the
# output directory used by build-deb.sh).
dist/
debian/rowhammer/
debian/.debhelper/
debian/debhelper-build-stamp
debian/files
debian/*.substvars
debian/*.log

# Demo-clip toolchain intermediates (tools/demo/make_demos.py)
tools/demo/.build/

# Python bytecode caches
__pycache__/
*.pyc
